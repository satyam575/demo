spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://ep-solitary-surf-a14ob7dy.ap-southeast-1.aws.neon.tech/shadisaga?sslmode=require&channelBinding=require
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_dJ4rf8ZStaEK}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        transaction:
          jta:
            platform: none

msg91:
  baseUrl: https://api.msg91.com
  authKey: ${MSG91_AUTH_KEY:373104Aaj9SEapTk68bd8915P1}
  widgetId: ${MSG91_WIDGET_ID:3569676f4a73333734343737}
  otp:
    generatePath: /api/v5/widget/sendOtp
    verifyPath: /api/v5/widget/verifyOtp
    templateId: ${MSG91_TEMPLATE_ID:620a2bb06e662e0ebf02b9e2}

jwt:
  secret: ${JWT_SECRET:mySecretKey123456789012345678901234567890}
  issuer: demo-app
  accessTtlMinutes: 90
  refreshTtlDays: 10

server:
  port: 8080

logging:
  level:
    com.example.demo: DEBUG
