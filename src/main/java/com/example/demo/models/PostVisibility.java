package com.example.demo.models;

public enum PostVisibility {
    PUBLIC,      // Visible to all wedding members
    PRIVATE,     // Visible only to author
    FRIENDS      // Visible to friends (future feature)
}
